pipeline {
  agent {
    label 'master'
  }
  stages {
    stage('Initialize') {
      steps {
        echo 'Placeholder.'
      }
    }
    stage('build') {
      steps {
        sh 'echo build'
      }
    }
  }
}